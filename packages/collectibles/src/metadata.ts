import { createProofDigest, pseudonymousHolderSubject } from './proof-digest.js';
import type { AttendanceCredential, Collection, MomentEventMetadata, MomentMetadata } from './types.js';
export function buildMomentMetadata(input: { collection: Collection; event: MomentEventMetadata; credential: AttendanceCredential; verificationBaseUrl: string; holderSalt: string; now: string; onchainNetwork?: 'algorand'; }): MomentMetadata { const subject = pseudonymousHolderSubject(input.credential.userId, input.event.id, input.holderSalt); const proofDigest = createProofDigest({ collectionId: input.collection.id, eventId: input.event.id, credentialId: input.credential.id, subject, role: input.credential.role }); const proofId = proofDigest.replace('sha256:', '').slice(0, 32); return { schema:'hashpass-moment-v1', name: input.collection.name, description: input.collection.description, type:'proof_of_attendance', event: input.event, issuer:{ name:'HashPass', eventOrganizer:'Blockchain Summit Latam' }, holder:{ subject, role: input.credential.role }, attendance:{ status:'verified', method: input.credential.verificationMethod, verifiedAt: input.credential.verifiedAt }, artwork:{ image: input.collection.artworkUri }, proof:{ digest: proofDigest, verificationUrl: `${input.verificationBaseUrl.replace(/\/$/, '')}/moments/verify/${proofId}` }, onchain:{ status:'claimable_onchain', network: input.onchainNetwork ?? 'algorand', standard:'ARC-72', tokenId:null, ownerAddress:null, transactionId:null }, metadataVersion:1, sourceSystem:'hashpass' }; }

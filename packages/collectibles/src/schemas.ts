import type { MomentMetadata } from './types.js';
export function validateMomentMetadata(metadata: MomentMetadata): string[] { const errors: string[] = []; if (metadata.schema !== 'hashpass-moment-v1') errors.push('schema must be hashpass-moment-v1'); if (!metadata.name) errors.push('name is required'); if (!metadata.event?.id) errors.push('event.id is required'); if (!metadata.artwork?.image) errors.push('artwork.image is required'); if (!metadata.proof?.digest?.startsWith('sha256:')) errors.push('proof.digest must be sha256'); if (metadata.onchain.standard !== 'ARC-72') errors.push('onchain.standard must be ARC-72'); return errors; }

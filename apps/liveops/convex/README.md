# Convex operational plane

Run `pnpm exec convex dev` from `apps/liveops` to generate `convex/_generated` and configure `NEXT_PUBLIC_CONVEX_URL`. The schema keeps all operational entities event-scoped. `liveops.ts` implements reactive state, deterministic simulator ingestion, incident creation, constrained operator decisions, verification, and audit logging.

import React from 'react'; import { View, Text, TouchableOpacity, StyleSheet } from 'react-native';
export type MomentFilter = 'all'|'hashpass'|'poap'|'claimable'|'attendance';
export default function MomentFilterBar({ value, onChange }: { value: MomentFilter; onChange(v: MomentFilter): void }) { const tabs: [MomentFilter,string][] = [['all','All'],['hashpass','HashPass Moments'],['poap','Legacy POAPs'],['claimable','Claimable'],['attendance','Verified Attendance']]; return <View style={styles.row}>{tabs.map(([id,label]) => <TouchableOpacity key={id} onPress={() => onChange(id)} style={[styles.tab, value===id && styles.active]}><Text style={[styles.text, value===id && styles.activeText]}>{label}</Text></TouchableOpacity>)}</View>; }
const styles = StyleSheet.create({ row:{ flexDirection:'row', flexWrap:'wrap', gap:8, marginBottom:16 }, tab:{ borderWidth:1, borderColor:'#334155', borderRadius:999, paddingHorizontal:10, paddingVertical:7 }, active:{ backgroundColor:'#00A9E0', borderColor:'#00A9E0' }, text:{ color:'#94A3B8', fontSize:12, fontWeight:'700' }, activeText:{ color:'#06111F' } });
